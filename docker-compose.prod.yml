services:
  scheduler:
    image: ghcr.io/dragonl641/invest-brief:latest
    volumes:
      - ./config.json:/app/config.json:ro
      - ./.env:/app/.env:ro
      - ./logs:/app/logs
      - ./reports:/app/reports
    restart: unless-stopped

[project]
name = "invest-brief"
version = "0.1.0"
description = "Personalized investment briefing via email"
requires-python = ">=3.10"
dependencies = [
    "yfinance",
    "requests",
    "anthropic",
    "python-dotenv",
    "matplotlib",
    "croniter",
    "pydantic",
    "akshare",
    "finvizfinance>=1.3.0",
]

[project.scripts]
invest-brief = "run:main"

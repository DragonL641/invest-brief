{
  "_schedule_note": "Scheduler honors the FIRST cron of the FIRST enabled market (us preferred). Macro pipeline always merges US+CN, so only one schedule entry is needed.",
  "markets": {
    "us": {
      "enabled": true,
      "schedule": [
        { "cron": "30 22 * * 1-5", "timezone": "Asia/Shanghai" }
      ]
    },
    "cn": {
      "enabled": true,
      "schedule": [
        { "cron": "30 10 * * 1-5", "timezone": "Asia/Shanghai" }
      ]
    }
  },
  "email_service": {
    "provider": "qq",
    "smtp_server": "smtp.qq.com",
    "smtp_port": 465,
    "sender_email": "YOUR_EMAIL@qq.com",
    "sender_name": "投资简报"
  },
  "recipients": [
    {
      "email": "recipient@example.com",
      "name": "Recipient1",
      "active": true,
      "language": "zh-CN"
    }
  ]
}
